# /* Copyright (c) SRI International 2002. */
SHELL = /bin/sh

# If Make doesn't predefine MAKE, set it here.
@SET_MAKE@

all: 
	@(cd src; $(MAKE))

clean:
	find . -name '.lazy-prover-trace' -exec rm '{}' ';'
	find . -name 'core' -exec rm '{}' ';'
	@(cd src; $(MAKE) clean)

